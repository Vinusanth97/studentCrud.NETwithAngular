Open "Frontend" in VS Code.
Run the command "npm install" , this will install all the dependencies.
Open "Backend" in Visual Studio
Change Server name in appsettings.json
Tools -> Nuget Package Manager Console -> Enter Command "update-database"
Now you can run the project
